server:
  port: 8084

spring:
  application:
    name: fee-service
  datasource:
    url: jdbc:postgresql://localhost:5432/school_erp
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
