server:
  port: ${PORT:8080}

spring:
  application:
    name: lms-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:prod}

anthropic:
  api:
    key: ${ANTHROPIC_API_KEY:default_dev_key}
