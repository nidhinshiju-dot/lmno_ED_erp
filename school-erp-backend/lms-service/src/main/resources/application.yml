server:
  port: 8082

spring:
  application:
    name: lms-service
  datasource:
    url: jdbc:postgresql://localhost:5432/school_erp
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

anthropic:
  api:
    key: ${ANTHROPIC_API_KEY:default_dev_key}
